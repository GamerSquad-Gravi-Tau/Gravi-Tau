using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpaceStationTurret : MonoBehaviour
{
    private int curveTParam;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private void FollowCurveWhenSpwan()
    {
        while (curveTParam > 1)
        {

        }
    }


    //turret
    //spwan enemy randomly in space (as the child of EnemyManger)
    //boss
}
